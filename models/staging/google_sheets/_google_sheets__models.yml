version: 2

models:
  - name: stg_google_sheets__budget
    config:
      contract:
        enforced: true
    columns:
      - name: _row
      - name: product_id
      - name: quantity
        tests:
          - not_null
        data_type: text
        constraints:
          - type: not_null
      - name: month
      - name: _fivetran_synced